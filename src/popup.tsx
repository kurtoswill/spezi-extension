import ReactDOM from "react-dom/client";
import "./styles.css";

const App = () => {
  return (
    <div>
      Spezi
    </div>
  );
};

export default App;

ReactDOM.createRoot(document.getElementById("root")!).render(<App />);
